import React from 'react';

const NewReview = () => {

    return (
        <div className="reviews new-review-container">
            <button>SUBMIT</button>
            <button>CLOSE</button>
        </div>
    );
};

export default NewReview;